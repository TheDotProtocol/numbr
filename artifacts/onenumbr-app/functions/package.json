{
  "name": "onenumbr-functions",
  "version": "1.0.0",
  "private": true,
  "description": "OneNumbr Cloud Functions (scaffold — populated in future prompts)",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
